{
  "name": "hello-world-node-typescript",
  "version": "0.0.1",
  "scripts": {
    "compile": "tsc",
    "watch": "tsc --watch",
    "run": "node dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@types/ws": "^8.5.10",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
